<div class="kb-template-selector--wrapper">
    <div class="kb-selectbox">
        <label>
            Modultemplate
            <select class="kb-template-select" data-kb-rel="{{ module.instance_id }}" name="{{ module.instance_id }}[viewfile]">
                {% for template in templates %}
                    <option {{ template.selected }} value="{{ template.filteredfile }}">{{ template.nicename }}</option>
                {% endfor %}
            </select></label>
    </div>

</div>