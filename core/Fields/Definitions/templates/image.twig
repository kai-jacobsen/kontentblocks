{{ field.label() }}
<div class='kb-field-image-wrapper'>
    <div class='kb-js-add-image kb-field-image-container'>

        {% if (field.getArg('previewSize')) %}
            <img src='{{ image.getSize(field.getArg('previewSize')) }}'>
        {% else %}
            <img src='{{ image.getSize('thumbnail') }}'>
        {% endif %}

    </div>

    {% if (field.getArg('hideMeta') == true) %}
    <div style="display: none;">
        {% endif %}

        <div class="kb-js-image-meta-wrapper">
            <label>{{ i18n.title }} </label>
            <input class='kb-js-image-title kb-observe' type="text" name='{{ field.getFieldName( 'details', 'title') }}'
                   value='{{ value.details.title }}'>
            <label>{{ i18n.description }}</label>
            <input class='kb-js-image-description kb-observe' type="text" name='{{ field.getFieldName( 'details', 'description') }}'
                   value='{{ value.details.description }}'>
        </div>
        {% if (field.getArg('hideMeta') == true) %}

    </div>
    {% endif %}
    <div class="kb-field-image--footer">
        <a class="button kb-js-reset-image">Reset</a>
    </div>
    <input class='kb-js-image-id' type='hidden' name='{{ field.getFieldName( 'id' ) }}' value='{{ value.id }}'>
    <input class='kb-js-image-alt' type='hidden' name='{{ field.getFieldName( 'details', 'id' ) }}' value='{{ value.details.alt }}'>
</div>
{{ field.description() }}
