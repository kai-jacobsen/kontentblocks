<div class="otimes-field--stage">
    <span class="js-oday-activate-split">Aufteilen</span>
    <table class="otimes-table">
        <tbody>
        <tr>
            <td class="oday">{{ i18n.mon.short }}</td>
            <td class="oday-open">
                <input name="{{ field.getFieldName('mon', '0', 'open') }}" type="text" value="{{ value.mon.0.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til">{{ i18n.until }}</td>
            <td class="oday-close">
                <input name="{{ field.getFieldName('mon', '0', 'close') }}" type="text" value="{{ value.mon.0.close }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til">{{ i18n.and }}</td>
            <td class="oday-open oday-split">
                <input name="{{ field.getFieldName('mon', '1', 'open') }}" type="text" value="{{ value.mon.1.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til oday-split">{{ i18n.until }}</td>
            <td class="oday-close oday-split">
                <input name="{{ field.getFieldName('mon', '1', 'close') }}" type="text" value="{{ value.mon.1.close }}"
                       class="kb-ot-timepicker">
            </td>
        </tr>


        <tr>
            <td class="oday">{{ i18n.tue.short }}</td>
            <td class="oday-open">
                <input name="{{ field.getFieldName('tue', '0', 'open') }}" type="text" value="{{ value.tue.0.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til">{{ i18n.until }}</td>
            <td class="oday-close">
                <input name="{{ field.getFieldName('tue', '0', 'close') }}" type="text" value="{{ value.tue.0.close }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til add-split">{{ i18n.and }}</td>
            <td class="oday-open oday-split">
                <input name="{{ field.getFieldName('tue', '1', 'open') }}" type="text" value="{{ value.tue.1.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til oday-split">{{ i18n.until }}</td>
            <td class="oday-close oday-split">
                <input name="{{ field.getFieldName('tue', '1', 'close') }}" type="text" value="{{ value.tue.1.close }}"
                       class="kb-ot-timepicker">
            </td>
        </tr>

        <tr>
            <td class="oday">{{ i18n.wed.short }}</td>
            <td class="oday-open">
                <input name="{{ field.getFieldName('wed', '0', 'open') }}" type="text" value="{{ value.wed.0.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til">{{ i18n.until }}</td>
            <td class="oday-close">
                <input name="{{ field.getFieldName('wed', '0', 'close') }}" type="text" value="{{ value.wed.0.close }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til add-split">{{ i18n.and }}</td>
            <td class="oday-open oday-split">
                <input name="{{ field.getFieldName('wed', '1', 'open') }}" type="text" value="{{ value.wed.1.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til oday-split">{{ i18n.until }}</td>
            <td class="oday-close oday-split">
                <input name="{{ field.getFieldName('wed', '1', 'close') }}" type="text" value="{{ value.wed.1.close }}"
                       class="kb-ot-timepicker">
            </td>
        </tr>
        <tr>
            <td class="oday">{{ i18n.thu.short }}</td>
            <td class="oday-open">
                <input name="{{ field.getFieldName('thu', '0', 'open') }}" type="text" value="{{ value.thu.0.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til">{{ i18n.until }}</td>
            <td class="oday-close">
                <input name="{{ field.getFieldName('thu', '0', 'close') }}" type="text" value="{{ value.thu.0.close }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til add-split">{{ i18n.and }}</td>
            <td class="oday-open oday-split">
                <input name="{{ field.getFieldName('thu', '1', 'open') }}" type="text" value="{{ value.thu.1.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til oday-split">{{ i18n.until }}</td>
            <td class="oday-close oday-split">
                <input name="{{ field.getFieldName('thu', '1', 'close') }}" type="text" value="{{ value.thu.1.close }}"
                       class="kb-ot-timepicker">
            </td>
        </tr>
        <tr>
            <td class="oday">{{ i18n.fri.short }}</td>
            <td class="oday-open">
                <input name="{{ field.getFieldName('fri', '0', 'open') }}" type="text" value="{{ value.fri.0.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til">{{ i18n.until }}</td>
            <td class="oday-close">
                <input name="{{ field.getFieldName('fri', '0', 'close') }}" type="text" value="{{ value.fri.0.close }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til add-split">{{ i18n.and }}</td>
            <td class="oday-open oday-split">
                <input name="{{ field.getFieldName('fri', '1', 'open') }}" type="text" value="{{ value.fri.1.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til oday-split">{{ i18n.until }}</td>
            <td class="oday-close oday-split">
                <input name="{{ field.getFieldName('fri', '1', 'close') }}" type="text" value="{{ value.fri.1.close }}"
                       class="kb-ot-timepicker">
            </td>
        </tr>
        <tr>
            <td class="oday">{{ i18n.sat.short }}</td>
            <td class="oday-open">
                <input name="{{ field.getFieldName('sat', '0', 'open') }}" type="text" value="{{ value.sat.0.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til">{{ i18n.until }}</td>
            <td class="oday-close">
                <input name="{{ field.getFieldName('sat', '0', 'close') }}" type="text" value="{{ value.sat.0.close }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til add-split">{{ i18n.and }}</td>
            <td class="oday-open oday-split">
                <input name="{{ field.getFieldName('sat', '1', 'open') }}" type="text" value="{{ value.sat.1.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til oday-split">{{ i18n.until }}</td>
            <td class="oday-close oday-split">
                <input name="{{ field.getFieldName('sat', '1', 'close') }}" type="text" value="{{ value.sat.1.close }}"
                       class="kb-ot-timepicker">
            </td>
        </tr>
        <tr>
            <td class="oday">{{ i18n.sun.short }}</td>
            <td class="oday-open">
                <input name="{{ field.getFieldName('sun', '0', 'open') }}" type="text" value="{{ value.sun.0.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til">{{ i18n.until }}</td>
            <td class="oday-close">
                <input name="{{ field.getFieldName('sun', '0', 'close') }}" type="text" value="{{ value.sun.0.close }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til add-split">{{ i18n.and }}</td>
            <td class="oday-open oday-split">
                <input name="{{ field.getFieldName('sun', '1', 'open') }}" type="text" value="{{ value.sun.1.open }}"
                       class="kb-ot-timepicker">
            </td>
            <td class="oday-til oday-split">{{ i18n.until }}</td>
            <td class="oday-close oday-split">
                <input name="{{ field.getFieldName('sun', '1', 'close') }}" type="text" value="{{ value.sun.1.close }}"
                       class="kb-ot-timepicker">
            </td>
        </tr>
        </tbody>
    </table>
</div>