<label>
    {% if control.label %}
    <span class="customize-control-title">{{ control.label|esc_html }}</span>
    {% endif %}
    {% if control.description %}
    <span class="description customize-control-description"><?php echo $this->description; ?></span>
    {% endif %}
    <input data-kbf-link-url type="text" {{ control.input_attrs() }} value="{{ control.value|esc_attr }}" {{ control.link() }} />
    <input data-kbf-link-linktext type="text" value="{{ control.value|esc_attr }}" {{ control.link() }} />
    <a class='button kb-js-add-link'>add</a>
</label>